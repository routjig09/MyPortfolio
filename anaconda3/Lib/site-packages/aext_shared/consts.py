import os

"""
This environment variable is used by our custom implementation of the `contents-manager` found in ./contents_manager.py

It serves to identify and hide an important symlink location used by `aext_project_filebrowser_server` from the frontend
client, as well as prevent any conflicting deletion and renaming operations by users of `aext_cloud`.
"""
ANACONDA_PROJECTS_DIR = os.getenv("ANACONDA_PROJECTS_DIR", None)
